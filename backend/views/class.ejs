<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Meeting Room</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        video {
            width: 300px;
            border-radius: 8px;
            border: 2px solid #00f;
        }

        #videoDiv {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
        }
    </style>
</head>

<body>
    <header class="bg-dark text-white text-center py-4">
        <h2>Workout Video Meet</h2>
        <p>Share this link: <code id="roomLink"><%= "http://localhost:5000/" + RoomId %></code></p>
        <button class="btn btn-primary btn-sm" onclick="copyURL()">Copy Room Link</button>
    </header>
    <main class="container mt-4">
        <div id="videoDiv"></div>
    </main>

    <script>
        const roomID = "<%= RoomId %>";

        function copyURL() {
            navigator.clipboard.writeText(location.href).then(() => alert("Link copied!"));
        }
    </script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script src="/class.js"></script>
</body>

</html>